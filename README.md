Svatební web Verča&Honza
========================

## Local installation

```bash
$ bundle config --local path vendor
$ bundle install
```

## Deployment

```bash
$ bundle config --local path vendor
$ bundle config set --local deployment 'true'
$ bundle install
```
